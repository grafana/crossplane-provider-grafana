/*
Copyright 2022 Upbound Inc.
*/

// Code generated by upjet. DO NOT EDIT.

package v1alpha1

import (
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	"k8s.io/apimachinery/pkg/runtime/schema"

	v1common "github.com/crossplane/crossplane-runtime/v2/apis/common/v1"
	v1 "k8s.io/apiextensions-apiserver/pkg/apis/apiextensions/v1"
)

type ResourceInitParameters struct {

	// managed resources are locked from UI edits unless you opt in. Set to true to allow UI modifications; not supported by all resources.
	// Whether the resource can be edited from the Grafana UI. Set to `true` to allow UI modifications; not supported by all resources.
	AllowUIUpdates *bool `json:"allowUiUpdates,omitempty" tf:"allow_ui_updates,omitempty"`

	// style manifest, typically from yamldecode(file(...)) or jsondecode(file(...)). Must contain apiVersion, kind, metadata (with name or uid), and spec. If you start from an exported manifest, remove noisy server-managed metadata such as resourceVersion, generation, and managedFields, or import the resource first and use the normalized state shape. If metadata.namespace is set, it must match the namespace selected from provider org_id or stack_id / autodiscovery. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. The secure field must not be set here; use the top-level secure argument instead.
	// Kubernetes-style manifest, typically from `yamldecode(file(...))` or `jsondecode(file(...))`. Must contain `apiVersion`, `kind`, `metadata` (with `name` or `uid`), and `spec`. If you start from an exported manifest, remove noisy server-managed metadata such as `resourceVersion`, `generation`, and `managedFields`, or import the resource first and use the normalized state shape. If `metadata.namespace` is set, it must match the namespace selected from provider `org_id` or `stack_id` / autodiscovery. Top-level manifest fields are limited to `apiVersion`, `kind`, `metadata`, `spec`, and the ignored `status` field. The `secure` field must not be set here; use the top-level `secure` argument instead.
	Manifest *v1.JSON `json:"manifest,omitempty" tf:"manifest,omitempty"`

	// only) Write-only secure values map. Each key must contain exactly one of create or name; empty objects are invalid.
	// Write-only secure values map. Each key must contain exactly one of `create` or `name`; empty objects are invalid.
	Secure *v1.JSON `json:"secure,omitempty" tf:"secure,omitempty"`

	// apply secure values.
	SecureVersion *float64 `json:"secureVersion,omitempty" tf:"secure_version,omitempty"`
}

type ResourceObservation struct {

	// managed resources are locked from UI edits unless you opt in. Set to true to allow UI modifications; not supported by all resources.
	// Whether the resource can be edited from the Grafana UI. Set to `true` to allow UI modifications; not supported by all resources.
	AllowUIUpdates *bool `json:"allowUiUpdates,omitempty" tf:"allow_ui_updates,omitempty"`

	// (String) The API resource UUID assigned by Grafana. This is not used for import; import uses the object name stored in metadata.uid.
	ID *string `json:"id,omitempty" tf:"id,omitempty"`

	// style manifest, typically from yamldecode(file(...)) or jsondecode(file(...)). Must contain apiVersion, kind, metadata (with name or uid), and spec. If you start from an exported manifest, remove noisy server-managed metadata such as resourceVersion, generation, and managedFields, or import the resource first and use the normalized state shape. If metadata.namespace is set, it must match the namespace selected from provider org_id or stack_id / autodiscovery. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. The secure field must not be set here; use the top-level secure argument instead.
	// Kubernetes-style manifest, typically from `yamldecode(file(...))` or `jsondecode(file(...))`. Must contain `apiVersion`, `kind`, `metadata` (with `name` or `uid`), and `spec`. If you start from an exported manifest, remove noisy server-managed metadata such as `resourceVersion`, `generation`, and `managedFields`, or import the resource first and use the normalized state shape. If `metadata.namespace` is set, it must match the namespace selected from provider `org_id` or `stack_id` / autodiscovery. Top-level manifest fields are limited to `apiVersion`, `kind`, `metadata`, `spec`, and the ignored `status` field. The `secure` field must not be set here; use the top-level `secure` argument instead.
	Manifest *v1.JSON `json:"manifest,omitempty" tf:"manifest,omitempty"`

	// only) Write-only secure values map. Each key must contain exactly one of create or name; empty objects are invalid.
	// Write-only secure values map. Each key must contain exactly one of `create` or `name`; empty objects are invalid.
	Secure *v1.JSON `json:"secure,omitempty" tf:"secure,omitempty"`

	// apply secure values.
	SecureVersion *float64 `json:"secureVersion,omitempty" tf:"secure_version,omitempty"`
}

type ResourceParameters struct {

	// managed resources are locked from UI edits unless you opt in. Set to true to allow UI modifications; not supported by all resources.
	// Whether the resource can be edited from the Grafana UI. Set to `true` to allow UI modifications; not supported by all resources.
	// +kubebuilder:validation:Optional
	AllowUIUpdates *bool `json:"allowUiUpdates,omitempty" tf:"allow_ui_updates,omitempty"`

	// style manifest, typically from yamldecode(file(...)) or jsondecode(file(...)). Must contain apiVersion, kind, metadata (with name or uid), and spec. If you start from an exported manifest, remove noisy server-managed metadata such as resourceVersion, generation, and managedFields, or import the resource first and use the normalized state shape. If metadata.namespace is set, it must match the namespace selected from provider org_id or stack_id / autodiscovery. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. The secure field must not be set here; use the top-level secure argument instead.
	// Kubernetes-style manifest, typically from `yamldecode(file(...))` or `jsondecode(file(...))`. Must contain `apiVersion`, `kind`, `metadata` (with `name` or `uid`), and `spec`. If you start from an exported manifest, remove noisy server-managed metadata such as `resourceVersion`, `generation`, and `managedFields`, or import the resource first and use the normalized state shape. If `metadata.namespace` is set, it must match the namespace selected from provider `org_id` or `stack_id` / autodiscovery. Top-level manifest fields are limited to `apiVersion`, `kind`, `metadata`, `spec`, and the ignored `status` field. The `secure` field must not be set here; use the top-level `secure` argument instead.
	// +kubebuilder:validation:Optional
	Manifest *v1.JSON `json:"manifest,omitempty" tf:"manifest,omitempty"`

	// only) Write-only secure values map. Each key must contain exactly one of create or name; empty objects are invalid.
	// Write-only secure values map. Each key must contain exactly one of `create` or `name`; empty objects are invalid.
	// +kubebuilder:validation:Optional
	Secure *v1.JSON `json:"secure,omitempty" tf:"secure,omitempty"`

	// apply secure values.
	// +kubebuilder:validation:Optional
	SecureVersion *float64 `json:"secureVersion,omitempty" tf:"secure_version,omitempty"`
}

// ResourceSpec defines the desired state of Resource
type ResourceSpec struct {
	v1common.ResourceSpec `json:",inline"`
	ForProvider           ResourceParameters `json:"forProvider"`
	// THIS IS A BETA FIELD. It will be honored
	// unless the Management Policies feature flag is disabled.
	// InitProvider holds the same fields as ForProvider, with the exception
	// of Identifier and other resource reference fields. The fields that are
	// in InitProvider are merged into ForProvider when the resource is created.
	// The same fields are also added to the terraform ignore_changes hook, to
	// avoid updating them after creation. This is useful for fields that are
	// required on creation, but we do not desire to update them after creation,
	// for example because of an external controller is managing them, like an
	// autoscaler.
	InitProvider ResourceInitParameters `json:"initProvider,omitempty"`
}

// ResourceStatus defines the observed state of Resource.
type ResourceStatus struct {
	v1common.ResourceStatus `json:",inline"`
	AtProvider              ResourceObservation `json:"atProvider,omitempty"`
}

// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:storageversion

// Resource is the Schema for the Resources API.  The resource is still experimental; diffing semantics are subject to change - feedback welcome in https://github. This resource accepts a Kubernetes-style manifest as the single source of truth for the resource definition. Only namespaced App Platform kinds are supported. The provider autodiscovers the namespace from /bootdata on every operation. If autodiscovery does not find a cloud stack namespace, the provider falls back to the explicit stack_id and then org_id provider settings. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. If metadata.namespace is configured, it must match the provider-selected namespace. Inside manifest.metadata, both Kubernetes name and uid are accepted as input aliases for the object identifier.11 or later. Reads refresh managed drift from the API. Import format:
// +kubebuilder:printcolumn:name="SYNCED",type="string",JSONPath=".status.conditions[?(@.type=='Synced')].status"
// +kubebuilder:printcolumn:name="READY",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="EXTERNAL-NAME",type="string",JSONPath=".metadata.annotations.crossplane\\.io/external-name"
// +kubebuilder:printcolumn:name="AGE",type="date",JSONPath=".metadata.creationTimestamp"
// +kubebuilder:resource:scope=Cluster,categories={crossplane,managed,grafana}
type Resource struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`
	Spec              ResourceSpec   `json:"spec"`
	Status            ResourceStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// ResourceList contains a list of Resources
type ResourceList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []Resource `json:"items"`
}

// Repository type metadata.
var (
	Resource_Kind             = "Resource"
	Resource_GroupKind        = schema.GroupKind{Group: CRDGroup, Kind: Resource_Kind}.String()
	Resource_KindAPIVersion   = Resource_Kind + "." + CRDGroupVersion.String()
	Resource_GroupVersionKind = CRDGroupVersion.WithKind(Resource_Kind)
)

func init() {
	SchemeBuilder.Register(&Resource{}, &ResourceList{})
}
