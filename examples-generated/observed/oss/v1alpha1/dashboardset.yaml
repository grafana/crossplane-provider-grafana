apiVersion: oss.grafana.o.crossplane.io/v1alpha1
kind: DashboardSet
metadata:
  name: example-dashboardset
  namespace: default
spec:
  forProvider:
    folderUIDs: # TODO
    limit: 0
    orgID: "example-org_id"
    tags: # TODO
  providerConfigRef:
    name: example
