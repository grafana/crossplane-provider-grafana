apiVersion: cloud.grafana.o.crossplane.io/v1alpha1
kind: Stack
metadata:
  name: example-stack
  namespace: default
spec:
  forProvider:
    slug: "example-slug"
  providerConfigRef:
    name: example
