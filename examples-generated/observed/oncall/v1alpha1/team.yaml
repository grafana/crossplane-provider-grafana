apiVersion: oncall.grafana.o.crossplane.io/v1alpha1
kind: Team
metadata:
  name: example-team
  namespace: default
spec:
  forProvider:
    name: "example-name"
  providerConfigRef:
    name: example
