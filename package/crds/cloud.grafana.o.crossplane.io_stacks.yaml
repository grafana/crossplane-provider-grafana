---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stacks.cloud.grafana.o.crossplane.io
spec:
  group: cloud.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Stack
    listKind: StackList
    plural: stacks
    singular: stack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Stack is an observe-only resource backed by the grafana_cloud_stack
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: StackParameters defines the input parameters for the
                  grafana_cloud_stack data source.
                properties:
                  slug:
                    description: |-
                      Subdomain that the Grafana instance will be available at (i.e. setting slug to “<stack_slug>” will make the instance
                      available at “https://<stack_slug>.grafana.net".
                    type: string
                required:
                - slug
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: StackObservation holds the observed (computed) fields
                  from the grafana_cloud_stack data source.
                properties:
                  alertmanagerIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Alertmanager instances (Optional)
                    type: string
                  alertmanagerName:
                    description: Name of the Alertmanager instance configured for
                      this stack.
                    type: string
                  alertmanagerStatus:
                    description: Status of the Alertmanager instance configured for
                      this stack.
                    type: string
                  alertmanagerURL:
                    description: Base URL of the Alertmanager instance configured
                      for this stack.
                    type: string
                  alertmanagerUserID:
                    description: User ID of the Alertmanager instance configured for
                      this stack.
                    format: int64
                    type: integer
                  clusterName:
                    description: Name of the cluster where this stack resides.
                    type: string
                  clusterSlug:
                    description: Slug of the cluster where this stack resides.
                    type: string
                  deleteProtection:
                    description: Whether to enable delete protection for the stack,
                      preventing accidental deletion.
                    type: boolean
                  description:
                    description: Description of stack.
                    type: string
                  fleetManagementName:
                    description: Name of the Fleet Management instance configured
                      for this stack.
                    type: string
                  fleetManagementPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Fleet Management when using
                      AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Fleet Management when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  fleetManagementPrivateConnectivityInfoRegions:
                    description: Regions for Fleet Management when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoServiceName:
                    description: Service Name for Fleet Management when using AWS
                      PrivateLink (only for AWS stacks)
                    type: string
                  fleetManagementStatus:
                    description: Status of the Fleet Management instance configured
                      for this stack.
                    type: string
                  fleetManagementURL:
                    description: Base URL of the Fleet Management instance configured
                      for this stack.
                    type: string
                  fleetManagementUserID:
                    description: User ID of the Fleet Management instance configured
                      for this stack.
                    format: int64
                    type: integer
                  grafanasIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the grafana instance (Optional)
                    type: string
                  graphiteIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Graphite instance (Optional)
                    type: string
                  graphiteName:
                    type: string
                  graphitePrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Graphite when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Graphite when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  graphitePrivateConnectivityInfoRegions:
                    description: Regions for Graphite when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoServiceName:
                    description: Service Name for Graphite when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  graphiteStatus:
                    type: string
                  graphiteURL:
                    type: string
                  graphiteUserID:
                    format: int64
                    type: integer
                  influxURL:
                    description: Base URL of the InfluxDB instance configured for
                      this stack. The username is the same as the metrics' (`prometheus_user_id`
                      attribute of this resource). See https://grafana.com/docs/grafana-cloud/send-data/metrics/metrics-influxdb/push-from-telegraf/
                      for docs on how to use this.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: 'A map of labels to assign to the stack. Label keys
                      and values must match the following regexp: "^[a-zA-Z0-9/\\-._]+$"
                      and stacks cannot have more than 10 labels.'
                    type: object
                  logsIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Logs instance (Optional)
                    type: string
                  logsName:
                    type: string
                  logsPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Logs when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Logs when using AWS PrivateLink (only
                      for AWS stacks)
                    type: string
                  logsPrivateConnectivityInfoRegions:
                    description: Regions for Logs when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoServiceName:
                    description: Service Name for Logs when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  logsStatus:
                    type: string
                  logsURL:
                    type: string
                  logsUserID:
                    format: int64
                    type: integer
                  name:
                    description: Name of stack. Conventionally matches the url of
                      the instance (e.g. `<stack_slug>.grafana.net`).
                    type: string
                  oncallAPIURL:
                    description: Base URL of the OnCall API instance configured for
                      this stack.
                    type: string
                  orgID:
                    description: Organization id to assign to this stack.
                    format: int64
                    type: integer
                  orgName:
                    description: Organization name to assign to this stack.
                    type: string
                  orgSlug:
                    description: Organization slug to assign to this stack.
                    type: string
                  otlpPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for OTLP when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for OTLP when using AWS PrivateLink (only
                      for AWS stacks)
                    type: string
                  otlpPrivateConnectivityInfoRegions:
                    description: Regions for OTLP when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoServiceName:
                    description: Service Name for OTLP when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  otlpURL:
                    description: Base URL of the OTLP instance configured for this
                      stack. The username is the stack's ID (`id` attribute of this
                      resource). See https://grafana.com/docs/grafana-cloud/send-data/otlp/send-data-otlp/
                      for docs on how to use this.
                    type: string
                  pdcAPIPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for PDC's API when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcAPIPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for PDC's API when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  pdcAPIPrivateConnectivityInfoRegions:
                    description: Regions for PDC's API when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcAPIPrivateConnectivityInfoServiceName:
                    description: Service Name for PDC's API when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  pdcGatewayPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for PDC's Gateway when using AWS
                      PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for PDC's Gateway when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  pdcGatewayPrivateConnectivityInfoRegions:
                    description: Regions for PDC's Gateway when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoServiceName:
                    description: Service Name for PDC's Gateway when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  profilesIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Profiles instance (Optional)
                    type: string
                  profilesName:
                    type: string
                  profilesPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Profiles when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Profiles when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  profilesPrivateConnectivityInfoRegions:
                    description: Regions for Profiles when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoServiceName:
                    description: Service Name for Profiles when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  profilesStatus:
                    type: string
                  profilesURL:
                    type: string
                  profilesUserID:
                    format: int64
                    type: integer
                  prometheusIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Prometheus instance (Optional)
                    type: string
                  prometheusName:
                    description: Prometheus name for this instance.
                    type: string
                  prometheusPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Prometheus when using AWS
                      PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Prometheus when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  prometheusPrivateConnectivityInfoRegions:
                    description: Regions for Prometheus when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoServiceName:
                    description: Service Name for Prometheus when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  prometheusRemoteEndpoint:
                    description: Use this URL to query hosted metrics data e.g. Prometheus
                      data source in Grafana
                    type: string
                  prometheusRemoteWriteEndpoint:
                    description: Use this URL to send prometheus metrics to Grafana
                      cloud
                    type: string
                  prometheusStatus:
                    description: Prometheus status for this instance.
                    type: string
                  prometheusURL:
                    description: Prometheus url for this instance.
                    type: string
                  prometheusUserID:
                    description: Prometheus user ID. Used for e.g. remote_write.
                    format: int64
                    type: integer
                  regionSlug:
                    description: The region this stack is deployed to.
                    type: string
                  status:
                    description: Status of the stack.
                    type: string
                  tracesIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Traces instance (Optional)
                    type: string
                  tracesName:
                    type: string
                  tracesPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Traces when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Traces when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  tracesPrivateConnectivityInfoRegions:
                    description: Regions for Traces when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoServiceName:
                    description: Service Name for Traces when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  tracesStatus:
                    type: string
                  tracesURL:
                    description: Base URL of the Traces instance configured for this
                      stack. To use this in the Tempo data source in Grafana, append
                      `/tempo` to the URL.
                    type: string
                  tracesUserID:
                    format: int64
                    type: integer
                  url:
                    description: Custom URL for the Grafana instance. Must have a
                      CNAME setup to point to `.grafana.net` before creating the stack
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
