---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stacks.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Stack
    listKind: StackList
    plural: stacks
    singular: stack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Stack is the Schema for the Stacks API. Manages the Asserts
          Stack configuration. This resource configures the Asserts stack with the
          required API tokens for integration with Grafana Cloud services. It performs
          the full onboarding flow: Provisions API tokensConfigures datasets (auto-detected
          or manually specified)Enables the stack By default, datasets are auto-configured
          based on detected metrics. To manually configure datasets (e.g., when using
          non-standard label names), use the dataset block. The cloud_access_policy_token
          is used internally for GCom API access, Mimir metrics authentication, and
          assertion detector webhook authentication. Create a Cloud Access Policy
          with the following scopes: stacks:read, metrics:read, metrics:write. The
          grafana_token is a Grafana Service Account token used for installing dashboards
          and Grafana Managed Alerts.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackSpec defines the desired state of Stack
            properties:
              forProvider:
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Cloud Access Policy token with the following scopes: stacks:read, metrics:read, metrics:write. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                      A Grafana Cloud Access Policy token with the following scopes: `stacks:read`, `metrics:read`, `metrics:write`. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  dataset:
                    description: |-
                      detection. Use this when your metrics use non-standard label names (e.g., a custom environment label). (see below for nested schema)
                      Manual dataset configuration. When specified, datasets are configured manually instead of using auto-detection. Use this when your metrics use non-standard label names (e.g., a custom environment label).
                    items:
                      properties:
                        disabledVendors:
                          description: |-
                            (List of String) List of vendors to disable for this dataset.
                            List of vendors to disable for this dataset.
                          items:
                            type: string
                          type: array
                        filterGroup:
                          description: |-
                            (Block List) Filter groups for this dataset. Use when you need custom label mappings. (see below for nested schema)
                            Filter groups for this dataset. Use when you need custom label mappings.
                          items:
                            properties:
                              envLabel:
                                description: |-
                                  (String) The metric label name used for environment (e.g., env, environment, deployment_environment). Defaults to standard labels if not set.
                                  The metric label name used for environment (e.g., `env`, `environment`, `deployment_environment`). Defaults to standard labels if not set.
                                type: string
                              envLabelValues:
                                description: |-
                                  (List of String) Specific values of the environment label to match.
                                  Specific values of the environment label to match.
                                items:
                                  type: string
                                type: array
                              envName:
                                description: |-
                                  (String) A friendly name for the environment.
                                  A friendly name for the environment.
                                type: string
                              filter:
                                description: |-
                                  (Block List) Additional metric filters. (see below for nested schema)
                                  Additional metric filters.
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) The label name to filter on.
                                        The label name to filter on.
                                      type: string
                                    operator:
                                      description: |-
                                        (String) The filter operator (e.g., =, !=, =~, !~).
                                        The filter operator (e.g., `=`, `!=`, `=~`, `!~`).
                                      type: string
                                    values:
                                      description: |-
                                        (List of String) The values to match.
                                        The values to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              siteLabel:
                                description: |-
                                  (String) The metric label name used for site/cluster.
                                  The metric label name used for site/cluster.
                                type: string
                              siteLabelValues:
                                description: |-
                                  (List of String) Specific values of the site label to match.
                                  Specific values of the site label to match.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) The dataset type. Available types: kubernetes, otel (App O11y), prometheus, aws. Note: kubernetes requires K8s Monitoring to be enabled, and otel requires Application Observability to be enabled on the stack.
                            The dataset type. Available types: `kubernetes`, `otel` (App O11y), `prometheus`, `aws`. Note: `kubernetes` requires K8s Monitoring to be enabled, and `otel` requires Application Observability to be enabled on the stack.
                          type: string
                      type: object
                    type: array
                  grafanaTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: dashboards:create, dashboards:write, dashboards:read, folders:create, folders:write, folders:read, folders:delete, datasources:read, datasources:query, alert.provisioning:write, alert.notifications.provisioning:write, alert.notifications:write, alert.rules:read, alert.rules:create, alert.rules:delete. Create using grafana_cloud_stack_service_account_token resource.
                      A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: `dashboards:create`, `dashboards:write`, `dashboards:read`, `folders:create`, `folders:write`, `folders:read`, `folders:delete`, `datasources:read`, `datasources:query`, `alert.provisioning:write`, `alert.notifications.provisioning:write`, `alert.notifications:write`, `alert.rules:read`, `alert.rules:create`, `alert.rules:delete`. Create using `grafana_cloud_stack_service_account_token` resource.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Cloud Access Policy token with the following scopes: stacks:read, metrics:read, metrics:write. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                      A Grafana Cloud Access Policy token with the following scopes: `stacks:read`, `metrics:read`, `metrics:write`. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  dataset:
                    description: |-
                      detection. Use this when your metrics use non-standard label names (e.g., a custom environment label). (see below for nested schema)
                      Manual dataset configuration. When specified, datasets are configured manually instead of using auto-detection. Use this when your metrics use non-standard label names (e.g., a custom environment label).
                    items:
                      properties:
                        disabledVendors:
                          description: |-
                            (List of String) List of vendors to disable for this dataset.
                            List of vendors to disable for this dataset.
                          items:
                            type: string
                          type: array
                        filterGroup:
                          description: |-
                            (Block List) Filter groups for this dataset. Use when you need custom label mappings. (see below for nested schema)
                            Filter groups for this dataset. Use when you need custom label mappings.
                          items:
                            properties:
                              envLabel:
                                description: |-
                                  (String) The metric label name used for environment (e.g., env, environment, deployment_environment). Defaults to standard labels if not set.
                                  The metric label name used for environment (e.g., `env`, `environment`, `deployment_environment`). Defaults to standard labels if not set.
                                type: string
                              envLabelValues:
                                description: |-
                                  (List of String) Specific values of the environment label to match.
                                  Specific values of the environment label to match.
                                items:
                                  type: string
                                type: array
                              envName:
                                description: |-
                                  (String) A friendly name for the environment.
                                  A friendly name for the environment.
                                type: string
                              filter:
                                description: |-
                                  (Block List) Additional metric filters. (see below for nested schema)
                                  Additional metric filters.
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) The label name to filter on.
                                        The label name to filter on.
                                      type: string
                                    operator:
                                      description: |-
                                        (String) The filter operator (e.g., =, !=, =~, !~).
                                        The filter operator (e.g., `=`, `!=`, `=~`, `!~`).
                                      type: string
                                    values:
                                      description: |-
                                        (List of String) The values to match.
                                        The values to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              siteLabel:
                                description: |-
                                  (String) The metric label name used for site/cluster.
                                  The metric label name used for site/cluster.
                                type: string
                              siteLabelValues:
                                description: |-
                                  (List of String) Specific values of the site label to match.
                                  Specific values of the site label to match.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) The dataset type. Available types: kubernetes, otel (App O11y), prometheus, aws. Note: kubernetes requires K8s Monitoring to be enabled, and otel requires Application Observability to be enabled on the stack.
                            The dataset type. Available types: `kubernetes`, `otel` (App O11y), `prometheus`, `aws`. Note: `kubernetes` requires K8s Monitoring to be enabled, and `otel` requires Application Observability to be enabled on the stack.
                          type: string
                      type: object
                    type: array
                  grafanaTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: dashboards:create, dashboards:write, dashboards:read, folders:create, folders:write, folders:read, folders:delete, datasources:read, datasources:query, alert.provisioning:write, alert.notifications.provisioning:write, alert.notifications:write, alert.rules:read, alert.rules:create, alert.rules:delete. Create using grafana_cloud_stack_service_account_token resource.
                      A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: `dashboards:create`, `dashboards:write`, `dashboards:read`, `folders:create`, `folders:write`, `folders:read`, `folders:delete`, `datasources:read`, `datasources:query`, `alert.provisioning:write`, `alert.notifications.provisioning:write`, `alert.notifications:write`, `alert.rules:read`, `alert.rules:create`, `alert.rules:delete`. Create using `grafana_cloud_stack_service_account_token` resource.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - cloudAccessPolicyTokenSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cloudAccessPolicyTokenSecretRef is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloudAccessPolicyTokenSecretRef)'
          status:
            description: StackStatus defines the observed state of Stack.
            properties:
              atProvider:
                properties:
                  dataset:
                    description: |-
                      detection. Use this when your metrics use non-standard label names (e.g., a custom environment label). (see below for nested schema)
                      Manual dataset configuration. When specified, datasets are configured manually instead of using auto-detection. Use this when your metrics use non-standard label names (e.g., a custom environment label).
                    items:
                      properties:
                        disabledVendors:
                          description: |-
                            (List of String) List of vendors to disable for this dataset.
                            List of vendors to disable for this dataset.
                          items:
                            type: string
                          type: array
                        filterGroup:
                          description: |-
                            (Block List) Filter groups for this dataset. Use when you need custom label mappings. (see below for nested schema)
                            Filter groups for this dataset. Use when you need custom label mappings.
                          items:
                            properties:
                              envLabel:
                                description: |-
                                  (String) The metric label name used for environment (e.g., env, environment, deployment_environment). Defaults to standard labels if not set.
                                  The metric label name used for environment (e.g., `env`, `environment`, `deployment_environment`). Defaults to standard labels if not set.
                                type: string
                              envLabelValues:
                                description: |-
                                  (List of String) Specific values of the environment label to match.
                                  Specific values of the environment label to match.
                                items:
                                  type: string
                                type: array
                              envName:
                                description: |-
                                  (String) A friendly name for the environment.
                                  A friendly name for the environment.
                                type: string
                              filter:
                                description: |-
                                  (Block List) Additional metric filters. (see below for nested schema)
                                  Additional metric filters.
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) The label name to filter on.
                                        The label name to filter on.
                                      type: string
                                    operator:
                                      description: |-
                                        (String) The filter operator (e.g., =, !=, =~, !~).
                                        The filter operator (e.g., `=`, `!=`, `=~`, `!~`).
                                      type: string
                                    values:
                                      description: |-
                                        (List of String) The values to match.
                                        The values to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              siteLabel:
                                description: |-
                                  (String) The metric label name used for site/cluster.
                                  The metric label name used for site/cluster.
                                type: string
                              siteLabelValues:
                                description: |-
                                  (List of String) Specific values of the site label to match.
                                  Specific values of the site label to match.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) The dataset type. Available types: kubernetes, otel (App O11y), prometheus, aws. Note: kubernetes requires K8s Monitoring to be enabled, and otel requires Application Observability to be enabled on the stack.
                            The dataset type. Available types: `kubernetes`, `otel` (App O11y), `prometheus`, `aws`. Note: `kubernetes` requires K8s Monitoring to be enabled, and `otel` requires Application Observability to be enabled on the stack.
                          type: string
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether the stack is currently enabled.
                      Whether the stack is currently enabled.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  status:
                    description: |-
                      (String) Current onboarding status of the stack.
                      Current onboarding status of the stack.
                    type: string
                  version:
                    description: |-
                      (Number) Configuration version number.
                      Configuration version number.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
